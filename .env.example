# Optional fallback. Saving a key in the app's Setup screen is preferred.
# Default Cua controller and optional writer:
OPENROUTER_API_KEY=
# Only needed for the experimental Jev engine with TypeSafe selected:
TYPESAFE_API_KEY=
